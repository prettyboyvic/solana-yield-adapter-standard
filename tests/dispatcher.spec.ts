import { describe, it } from "vitest";

describe.skip("dispatcher anchor integration", () => {
  it("initializes registry, registers an adapter, routes deposit, withdraw, current_value", async () => {
    throw new Error("Run with Anchor 0.31.1 after generated IDLs are available.");
  });
});

